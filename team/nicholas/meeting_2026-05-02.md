# Team Meeting — 2026-05-02 (evening)

Recorded by Nicholas's agent from a verbal team sync. This is a Nicholas-owned working doc — not the canonical project record. Sebastian owns final integration into `docs/`.

## Attendees

- Nicholas — detection / simulation / technical-prototype lead
- Sebastian — coordinator / integrator
- Arpit — frontend / dashboard / app architecture
- Birger — absent; subject-matter expert, to be consulted via SME question list (see below)

## Refinements to project framing

### Drone video feed perspective

The video feed is **first-person from the drone (FPV)**, not third-person observer. Confirmed by Arpit; Nicholas had been unsure.

### Detection approach — multi-signal cross-validation (NEW)

Identification is no longer single-signal (marker only). The pipeline should fuse multiple signals into a confidence score:

- **Visual drone-type classifier.** YOLO fine-tuned on public drone datasets, identifying friend-vs-foe by visual class. Arpit's framing.
- **Authenticated OSD marker** ("invisible glyph"). Dynamically rotating in real ops (~20s) to prevent enemy reverse-engineering. For demo, a simpler static or counter-based glyph is acceptable.
- **Bird's-eye stitching from cooperating FPV feeds.** Nicholas's idea — stitch multiple friendly FPV feeds into a synthetic top-down view, identify drones not in the friendly information graph as candidate foes. Viability depends on whether friendly drones actually share telemetry — SME question for Birger.
- **Acoustic.** Birger has prior knowledge that acoustic sensor coverage is abundant. Out of MVP, in pitch.
- **Infrasound.** Arpit's add. Out of MVP, in pitch.
- **Fiber-optic.** Birger noted: expensive, sparse. Pitch-only reference.
- **Crypto ESP32.** Sebastian found ~$10 blockchain-based unique drone identity hardware. **Open scope question** — original pitch was minimal-hardware; this introduces hardware. Resolution required before architecture lock.

### Confidence + risk-zone logic (NEW)

Operator dashboard semantics expand beyond the four-state taxonomy:

- Show confidence percentage (0–100%) per feed.
- Map confidence + zone risk to action recommendation:
  - **High confidence friend (~80%+):** don't engage, don't jam.
  - **High confidence foe (~≤20%):** engage / jam permitted.
  - **Mid (~50%):** human review by default; **but** in a high-risk zone the default flips to "treat as foe / do not assume friendly" to protect critical assets.
- Risk-zone is operator-settable per area / proximity to friendly assets.
- Safety boundary unchanged: identification aid only; the dashboard recommends, the operator decides.

### Evaluation metrics

Need confusion matrix per state — precision, recall, false-positive rate (friendly jammed), false-negative rate (foe missed). Keep tight; do not let metrics scope explode.

### Demo plan refinement

- Source clip: drone race or drone battle footage with two visually-distinguishable drone types.
- Fine-tune YOLO on those two classes.
- MVP = visual classifier + marker detection on the source video.
- Pitch covers the multi-modal vision (audio, infrasound, ESP32 in commentary, not in code).

## Action items

### Nicholas (filed in Priority Forge — `bluemark-fpv` workspace)

| ID | Task |
|----|------|
| NICK-006 | Draft SME question list for Birger (seed below). |
| NICK-007 | Draft candidate ML model set for multi-signal pipeline (seed below). |
| NICK-008 | Architecture proposal — multi-signal fusion + confidence + risk-zone + metrics (seed below). Coordinate with Sebastian. Blocked behind hardware-scope decision. |
| NICK-009 | Self-onboard on FPV / OSD / drone-type basics. Time-box 30 min. |

### Ask: Sebastian (via Codex)

1. **Arbitrate hardware-vs-software scope.** Original pitch said "minimal hardware impact"; cross-validation now wants ESP32 / acoustic / fiber / infrasound. Hackathon decision: in-scope or pitch-only? **NICK-008 is blocked on this.**
2. Once direction is locked, integrate multi-signal model + risk-zone logic into `docs/system_architecture.md` and `docs/demo_plan.md`. (Sebastian-owned files; I'm proposing here, not editing.)
3. Pitch language: the risk-zone "treat-as-foe in high-risk area" framing must stay inside the existing identification-aid safety boundary. The system *recommends*; the operator *engages*. Wording matters for safety review.

### Ask: Arpit (via Cursor)

Dashboard additions on top of the existing 4-state design:
- Confidence percentage per feed (0–100%).
- Risk-zone selector per area (low / mid / high).
- Action recommendation field (don't engage / human review / engage).
- Optional signal-source breakdown (which signals contributed: visual / marker / audio / etc.).

These extend the detector→dashboard schema we're locking in **NICK-005**. Suggest we re-sync that schema before either of us locks.

### Ask: Birger (SME)

1. Receive and answer the SME question list (NICK-006 deliverable; seed inline below — feel free to comment in `team/birger/changelog.md` if you spot misframings before I formalize).
2. If you have any prior fiber-optic / acoustic / EW-operations notes from past work, dropping them into `team/birger/` (any filename) would short-circuit the question loop.

## Seeds

### SME question list seed (NICK-006)

Drafted from gaps surfaced in this meeting. Subject to refinement.

1. What FPV drone models matter most on each side of the current conflict (Ukrainian custom builds, DJI variants, Russian Lancet/ZALA, etc.)?
2. What ground-station receiver hardware do EW teams typically have (SDR, video-grabber + Pi, milspec analog rig)? What constrains adding a software detector to that stack?
3. What does realistic FPV video degradation look like — analog noise patterns, dropouts, jamming artifacts, range distortion? Any reference clips you can point us at?
4. For OSD-style overlays — where in the frame are markers usually placed? What glyph size / luminance survives degradation but stays low-visibility to the enemy?
5. Are friendly Ukrainian FPV drones information-sharing in any structured way (telemetry mesh, AWACS-like coord, peer-to-peer)? This determines whether bird's-eye stitching is viable.
6. Acoustic detection — realistic range, false-positive rate, hardware footprint?
7. Fiber optics — when are these actually deployed? What's the signal-availability story?
8. Infrasound — Arpit raised this for "beams." Real-world deployment level?
9. False-positive vs false-negative tolerance in real ops — what's the accepted miss rate vs accepted friendly-jamming rate?
10. Realistic high-risk vs low-risk zone definitions — geo-fenced, threat-driven, or operator-set?
11. What metrics would a real EW commander want to see on a dashboard? Confidence %, signal breakdown, alert level, action recommendation?
12. Closest existing system (commercial or military) we should benchmark or reference in pitch?

### ML candidates seed (NICK-007)

| Layer | Candidate | MVP / Pitch | Notes |
|---|---|---|---|
| Visual classifier | YOLOv8 / v11 fine-tuned on drone classes | MVP | Need dataset: research VisDrone / AntiUAV / similar. |
| Marker detector | OpenCV `matchTemplate` / ArUco / DataMatrix | MVP | Deterministic-first per Nicholas's preference. ML fallback only if noise breaks it. |
| Multi-signal fusion | Logistic regression on signal scores | MVP | Interpretable, fast. Naive Bayes is the cheaper alternative. |
| Bird's-eye stitch | Feature-matching homography (SIFT/ORB) | Stretch | Only meaningful if friendly drones share telemetry — Birger Q5. |
| Audio | Spectral CNN on mel-spectrograms | Pitch only | Out of hackathon code scope. |
| Risk-zone modulation | Rules over zone metadata × confidence | MVP | Not ML; decision-tree logic. |

### Architecture proposal seed (NICK-008)

```
inputs:
  video_frame:     required
  audio_clip:      optional (pitch only for hackathon)
  marker_extract:  optional (from glyph region of video frame)
  zone_metadata:   required (operator-set)

per-signal scores → fusion → confidence in [0,1]
confidence × zone_risk → action_recommendation ∈ {dont_engage, human_review, engage}

output to dashboard:
  status:        Friendly Verified | Unknown | Corrupted | Possible Spoof
  confidence:    0..100
  action:        dont_engage | human_review | engage
  signals_used:  [visual, marker, audio?, ...]
  reason:        short string

metrics (offline eval):
  confusion_matrix per state
  precision, recall per state
  false_positive_rate (friendly jammed)
  false_negative_rate (foe missed)
```

**Blocked on:** Sebastian's hardware-vs-software scope decision. If hardware is out, drop ESP32 / fiber from the architecture diagram and keep them as pitch-only.

## Tensions to monitor

1. **Hardware scope creep.** ESP32 / acoustic / fiber / infrasound vs. original "minimal hardware" pitch. Sebastian decides.
2. **Demo simplification vs pitch credibility.** Real-world cryptographic marker rotation (~20s) is non-trivial; demo simplifies to static. Don't let the simplification undermine pitch credibility — frame production reqs explicitly in pitch.
3. **Metrics scope.** Confusion-matrix-driven evaluation can grow indefinitely. Pick the smallest set that sells the pitch (precision/recall per state + FP-rate + FN-rate is enough).
